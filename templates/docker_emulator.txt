{{ service_name }}:
    platform: {{ platform }}
    container_name: {{ container_name }}
    image: {{ image_name }}
    env_file:
      - ./env/.env
    ports:
      - "{{ grpc_port_host }}:8554"
      - "{{ adb_port_host }}:5555"
    networks:
      - frontend
{%- for line in optional_settings %}
    {{ line }}
{%- endfor %}
