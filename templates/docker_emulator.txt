{{ service_name }}:
    platform: "linux/amd64"
    container_name: {{ container_name }}
    image: {{ image_name }}
    devices: [/dev/kvm]
    env_file:
      - ./env/.env
    ports:
      - "{{ grpc_port_host }}:8554"
      - "{{ adb_port_host }}:5555"
    networks:
      - frontend
